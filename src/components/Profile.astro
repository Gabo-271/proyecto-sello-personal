---
import { Picture } from 'astro:assets';
import ProfilePicture from '../assets/profile.png';
import yo from '../assets/yo.png';

const profile = {
    name: 'Gabriel Soto Mendez.',
    title: 'Ingeniro En Sistemas ',
    location: 'Santiago, Chile',
    description: 'Soy estudiante de último año de Ingeniería en Informática con experiencia en desarrollo tanto frontend como backend adquirida en proyectos universitarios con clientes reales. Me adapto fácilmente a trabajar en equipos multidisciplinarios bajo metodologías ágiles y busco aportar soluciones escalables y eficientes mientras continúo aprendiendo y perfeccionando mis habilidades técnicas.',
    hobbies: 'Busco el equilibrio entre lo digital y lo tangible a través del diseño 3D y la carpintería, disfrutando del proceso de restaurar y construir muebles con mis propias manos.'
}
---

<div class="flex flex-col gap-6 items-center text-center sm:items-start sm:text-left">

    <div class="flex flex-col sm:flex-row gap-4 sm:items-center">

        <div class="mx-auto sm:mx-0 relative w-24 h-24" style="perspective: 1000px;">
            <div class="relative w-full h-full transition-transform duration-700 hover:[transform:rotateY(180deg)]" style="transform-style: preserve-3d;">
                <!-- Cara frontal -->
                <div class="absolute w-full h-full" style="backface-visibility: hidden;">
                    <Picture
                        src={ProfilePicture}
                        formats={['webp']}
                        width={96}
                        height={96}
                        alt={'Profile picture ' + profile.name}
                        title={profile.name}
                        class="w-full h-full rounded-xl object-cover border-2 border-primary"
                    />
                </div>
                <!-- Cara trasera -->
                <div class="absolute w-full h-full" style="backface-visibility: hidden; transform: rotateY(180deg);">
                    <Picture
                        src={yo}
                        formats={['webp']}
                        width={96}
                        height={96}
                        alt={'Alternative picture ' + profile.name}
                        title={profile.name}
                        class="w-full h-full rounded-xl object-cover border-2 border-primary"
                    />
                </div>
            </div>
        </div>

        <div>

            <h1 class="text-2xl font-extrabold mb-1 inline-block relative text-text-primary">
                {profile.name}
            </h1>

            <p class="text-sm mb-2 text-primary">
                {profile.title}
            </p>

            <p class="flex items-center justify-center sm:justify-start text-sm text-gray-400">
                <svg class="-mt-0.5 inline mr-2"fill=none height=14 stroke=currentColor stroke-linecap=round stroke-linejoin=round stroke-width=2 viewBox="0 0 24 24"width=14 xmlns=http://www.w3.org/2000/svg><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"></path><circle cx=12 cy=10 r=3></circle></svg>
                {profile.location}
            </p>

        </div>

    </div>

    <p class="text-sm leading-relaxed mt-4 text-gray-400">
        {profile.description}

    </p>
    <p class="text-sm leading-relaxed mt-4 text-gray-400">
        {profile.hobbies}
    </p>

</div>